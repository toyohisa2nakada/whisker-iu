"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RLTestGenerator = void 0;
const TestGenerator_1 = require("../../../testgenerator/TestGenerator");
const DeepQLearning_1 = require("../algorithms/DeepQLearning");
const RLEnvironment_1 = require("./RLEnvironment");
const RLTestSuite_1 = require("./RLTestSuite");
const StatementFitnessFunctionFactory_1 = require("../../../testcase/fitness/StatementFitnessFunctionFactory");
const BranchCoverageFitnessFunctionFactory_1 = require("../../../testcase/fitness/BranchCoverageFitnessFunctionFactory");
const CoverageDirectedDeepQLearning_1 = require("../algorithms/CoverageDirectedDeepQLearning");
const Arrays_1 = __importDefault(require("../../../utils/Arrays"));
class RLTestGenerator extends TestGenerator_1.TestGenerator {
    /**
     * Generates tests for the loaded Scratch project using Reinforcement Learning as an optimization algorithm.
     *
     * @returns a test suite containing all tests generated by the optimization algorithm.
     */
    generateTests() {
        return __awaiter(this, void 0, void 0, function* () {
            this._vmWrapper.vm.registerCoverageTracer(false);
            const algorithm = this.buildOptimizationAlgorithm();
            const archive = yield algorithm.findSolution();
            const trimmedArchive = Arrays_1.default.distinctObjects(archive.values());
            console.log(`Generated ${trimmedArchive.length} tests.`);
            return new RLTestSuite_1.RLTestSuite(trimmedArchive);
        });
    }
    /**
     * Builds and sets hyperparameter of the Reinforcement Learning algorithm to be used for generating tests.
     */
    buildOptimizationAlgorithm() {
        let algorithm;
        const parameter = this._config.getRLHyperparameter();
        if (this._config.getRLHyperparameter().rewardParameter.type === "coverage") {
            algorithm = new CoverageDirectedDeepQLearning_1.CoverageDirectedDeepQLearning(parameter, this._setupEnvironment());
        }
        else {
            algorithm = new DeepQLearning_1.DeepQLearning(this._config.getRLHyperparameter(), this._setupEnvironment());
        }
        algorithm.setFitnessFunctions(this._getCoverageObjectives());
        return algorithm;
    }
    /**
     * Initializes the Reinforcement Learning environment based on the specified parameter.
     */
    _setupEnvironment() {
        return new RLEnvironment_1.RLEnvironment(this._vmWrapper, this._config.getRLHyperparameter().environmentParameter);
    }
    /**
     * Collects the coverage objectives from the loaded Scratch project based on the specified coverage type.
     *
     * @returns a map containing the coverage objectives.
     */
    _getCoverageObjectives() {
        const { type, targets } = this._config.getRLHyperparameter().coverageObjectives;
        const coverageFactory = type === "statement" ?
            new StatementFitnessFunctionFactory_1.StatementFitnessFunctionFactory() : new BranchCoverageFitnessFunctionFactory_1.BranchCoverageFitnessFunctionFactory();
        const objectives = coverageFactory.extractFitnessFunctions(this._vmWrapper.vm, targets);
        const objectiveMap = new Map();
        for (let i = 0; i < objectives.length; i++) {
            objectiveMap.set(i, objectives[i]);
        }
        return objectiveMap;
    }
}
exports.RLTestGenerator = RLTestGenerator;
